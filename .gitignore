.venv/
__pycache__/
*.pyc
.vercel/
data/*.log
data/courses.deploy.db
data/courses.sealing.tmp.db
data/*.db-wal
data/*.db-shm

# Excel lock files Excel leaves behind while a workbook is open.
~$*

# NOT ignored: data/courses.db is the deploy artifact. CI rebuilds it, seals it
# and commits it, which is what triggers a Vercel deploy. See DEPLOY.md.
