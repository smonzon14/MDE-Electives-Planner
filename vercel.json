{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "functions": {
    "api/index.py": {
      "memory": 1024,
      "includeFiles": [
        "data/courses.db",
        "config.py",
        "mde_policy.yaml",
        "approved_lists.yaml",
        "server/**",
        "ingest/**"
      ]
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.py" }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" }
      ]
    },
    {
      "//": "There is no build step, so filenames are not content-hashed. If index.html were cached while app.js was refetched (or the reverse), the page would wire up against a DOM that no longer matches and break on load. Always revalidate: these files are a few KB and the ETag round trip is far cheaper than a broken deploy for returning users.",
      "source": "/(.*\\.(html|js|css)|)",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=0, must-revalidate" }
      ]
    }
  ]
}
